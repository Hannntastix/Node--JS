//install local package
//npm install <package-name>

//install global package
//Lalu, kapan kita menggunakan global package? Sebuah package harus diinstal secara global 
//hanya saat ia menyediakan perintah yang dapat dieksekusi dari CLI dan digunakan kembali di semua project. 
//Beberapa contoh package yang perlu diinstal secara global, antara lain:
// npm
// nodemon
// mocha
//npm install -g <package-name>

//Untuk memasang package sebagai devDependencies, kita bisa menambahkan parameter --save-dev pada perintah npm install.
//npm install <package-name> --save-dev

//npm uninstall <package-name>
// npm uninstall -h -> -h digunakan untuk melihat detail apa saja yng bisa digunakan dari setiap perintah